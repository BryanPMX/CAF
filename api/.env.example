# This file is a template for the environment variables.
# In a real project, copy this to a file named '.env' and fill in the secrets.
# The .env file should NEVER be committed to Git.

# Application Port
PORT=8080

# PostgreSQL Database Connection
DB_USER=user
DB_PASSWORD=password
DB_NAME=caf_db
DB_HOST=db # The service name from docker-compose.yml
DB_PORT=5432

# JWT Secret Key for signing tokens
JWT_SECRET=a_very_secret_key_that_should_be_changed

# AWS / LocalStack Configuration
AWS_REGION=us-east-1
S3_BUCKET=caf-client-documents
# For local development, this should point to the LocalStack container
S3_ENDPOINT=http://localstack:4566